<div>
    {{#each dbArticle}}
    <div class="container-fluid">
        <div class="card">
                <div class="card-header">
      <a href="https://www.nytimes.com/{{this.link}}">{{this.title}}</a>
    </div>
            <div clas="card-body">

    <div class="card-text">
        {{this.description}}
    </div>
    </div>
    </div>
    </div>

    {{/each}}
</div>